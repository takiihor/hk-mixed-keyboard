# Copy to keystore.properties for local release work only; the real file is ignored.
# CI/release automation should prefer the matching HKKBD_* environment variables.
storeFile=/absolute/path/outside/repository/hk-upload-key.jks
storePassword=replace-me
keyAlias=replace-me
keyPassword=replace-me
